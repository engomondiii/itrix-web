# itriX — Surface 1 (public site)
User-agent: *
Allow: /

# The funnel, the token-gated client pages, the portal and every proxy route are
# not for indexing. /c/* in particular is a PERSONALIZED page behind a capability
# token — indexing one would publish a private brief.
Disallow: /review
Disallow: /c/
Disallow: /workspace
Disallow: /sign-in
Disallow: /set-password
Disallow: /forgot-password
Disallow: /api

Sitemap: https://itrix.ai/sitemap.xml
