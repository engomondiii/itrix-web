import { redirect } from 'next/navigation';
/** Legacy waiting page retired. Review generation stays on the question surface. */
export default function PreparingPage() { redirect('/review/qualify'); }
