import { TechnologyHero } from '@/components/technology/TechnologyHero'; import { DisclosureLevelBadge } from '@/components/technology/DisclosureLevelBadge'; import { Card } from '@/components/ui/Card'; import { SectionLabel } from '@/components/ui/SectionLabel'; import { buildMetadata } from '@/components/seo/PageMeta'; import { TECHNOLOGIES } from '@/constants/products'; import { NDA_WARNINGS,NDA_WARNINGS_KO } from '@/lib/content/ndaWarnings'; import { routes } from '@/constants/routes'; import { LocalizedText as L } from '@/components/i18n/LocalizedText';
const t=TECHNOLOGIES.axiom; export const metadata=buildMetadata({title:'AXIOM',description:t.oneLiner,path:routes.axiom});
export default function AxiomPage(){return <><TechnologyHero eyebrow={<L en={t.gap} ko="대수적 상태–관측 간극"/>} title="AXIOM" expansion={<L en={t.expansion} ko="대수적 상태 표현"/>} lead={<L en={t.oneLiner} ko="계산을 대수적 상태로 표현하고 상태 전이, 투영된 관측, 숨은 상태 보존을 구분해 0인 관측을 0인 상태로 오해하지 않도록 합니다."/>}/><section className="section border-b border-border-medium bg-surface"><div className="container-page max-w-3xl"><div className="flex items-center gap-3"><DisclosureLevelBadge level="controlled_public"/></div><SectionLabel className="mt-8"><L en="The idea" ko="핵심 아이디어"/></SectionLabel><div className="reading mt-4"><p><L en={<>AXIOM treats a computation as an <strong>algebraic state</strong>. The state evolves through transitions; what you measure is a <em>projection</em> of that state, not the whole of it. The key consequence is simple and easy to get wrong: a zero observation does not mean a zero state. Information you can&apos;t see can still be preserved, carried forward, and reconstructed later.</>} ko={<>AXIOM은 계산을 <strong>대수적 상태</strong>로 다룹니다. 상태는 전이를 통해 변화하며 측정하는 것은 전체 상태가 아니라 그 상태의 <em>투영</em>입니다. 핵심은 관측이 0이라고 해서 상태가 0인 것은 아니라는 점입니다. 보이지 않는 정보도 보존되고 전달된 뒤 나중에 재구성될 수 있습니다.</>}/></p><p className="mt-4"><L en="Designing around this — preserving hidden state through transition and projection — is what lets downstream methods avoid redundant work and recover exact answers. It is the representation discipline the rest of the pipeline depends on." ko="전이와 투영 과정에서 숨은 상태를 보존하도록 설계하면 후속 방법이 중복 작업을 줄이고 정확한 결과를 복원할 수 있습니다. 이는 나머지 파이프라인이 의존하는 표현 원칙입니다."/></p></div><Card variant="sunken" className="mt-8"><span className="text-micro font-semibold uppercase tracking-[0.06em] text-ink-secondary"><L en="Public boundary" ko="공개 경계"/></span><p className="mt-2 text-secondary text-ink-secondary"><L en={NDA_WARNINGS.mechanism} ko={NDA_WARNINGS_KO.mechanism}/></p></Card></div></section></>}
