import { PageWrapper } from '@/components/layout/PageWrapper'; import { Card } from '@/components/ui/Card'; import { buildMetadata } from '@/components/seo/PageMeta'; import { routes } from '@/constants/routes'; import { brand } from '@/constants/brand'; import { LocalizedText as L } from '@/components/i18n/LocalizedText';
export const metadata=buildMetadata({title:'About',description:`${brand.legalEntity} — ${brand.positioning}.`,path:routes.about});
export default function AboutPage(){return <PageWrapper eyebrow={<L en="About itriX" ko="itriX 소개"/>} title={<L en="Computational AI infrastructure for sustainable AI" ko="지속 가능한 AI를 위한 계산 인프라"/>} grid lead={<L en={`${brand.legalEntity} exists to make computation worth scaling before anyone scales it.`} ko={`${brand.legalEntity}는 더 많은 하드웨어를 확장하기 전에 계산 자체를 확장할 가치가 있게 만드는 것을 목표로 합니다.`}/>}><div className="grid gap-10 md:grid-cols-[1.5fr_1fr]"><div className="reading"><p><L en="The industry's default answer to a hard computation is often more hardware. itriX starts earlier: first ask whether a system is observing what matters before expensive reasoning, and whether eligible computation is represented appropriately before execution. Any claimed benefit has to be established against a real baseline rather than assumed." ko="어려운 계산에 대한 업계의 기본 답은 흔히 더 많은 하드웨어입니다. itriX는 더 앞에서 시작합니다. 비용이 큰 추론 전에 시스템이 중요한 것을 제대로 관측하고 있는지, 그리고 실행 전에 적합한 계산이 올바르게 표현되어 있는지를 먼저 봅니다. 어떤 이점도 가정하지 않고 실제 기준선과 비교해 입증해야 합니다."/></p><p className="mt-4"><L en="That thesis is expressed through three products. ASTOP is the observation product and operationalizes the PRISM observation domain. ALPHA Compute is the independent software computational infrastructure product for eligible workloads. ALPHA Core is a separate optional hardware product considered only when validated software-layer evidence justifies deeper hardware implementation. PRISM, AXIOM, AXIOM-TENSOR, CRE, FQNM, and QNTA are technologies rather than separately sold products." ko="이 원칙은 세 제품으로 구현됩니다. ASTOP은 관측 제품이며 PRISM의 관측 영역을 제품화합니다. ALPHA Compute는 적합한 워크로드를 위한 독립 소프트웨어 계산 인프라 제품입니다. ALPHA Core는 검증된 소프트웨어 계층 근거가 더 깊은 하드웨어 구현을 정당화할 때만 고려하는 별도의 선택적 하드웨어 제품입니다. PRISM, AXIOM, AXIOM-TENSOR, CRE, FQNM, QNTA는 별도로 판매되는 제품이 아니라 기술입니다."/></p><p className="mt-4 font-medium text-structure-900">“{brand.thesis}”</p></div><Card variant="warm" className="self-start"><span className="text-micro font-semibold uppercase tracking-[0.06em] text-ink-secondary"><L en="The name" ko="이름의 의미"/></span><ul className="mt-3 flex flex-col gap-3">{brand.letters.map(l=><li key={l.meaning} className="flex items-baseline gap-3"><span className="font-mono text-web-h3 text-ink-primary">{l.char}</span><span className="text-secondary text-ink-secondary">{l.meaning}</span></li>)}</ul></Card></div></PageWrapper>}
