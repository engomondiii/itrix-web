'use client';
import { Card } from '@/components/ui/Card';
import { Badge } from '@/components/ui/Badge';
import { brand } from '@/constants/brand';
import { useNdaWarnings } from '@/lib/i18n/ndaLocale';
import { useLocaleStore } from '@/store/localeStore';
export function MediaKitPreview(){const locale=useLocaleStore(s=>s.locale);const w=useNdaWarnings();const ko=locale==='ko';return <section className="section border-b border-border-medium bg-surface"><div className="container-page max-w-3xl"><Badge tone="info">{ko?'미디어용':'For media'}</Badge><h2 className="mt-4 text-web-h2 text-structure-900">{ko?'정확한 공개 프레이밍':'Accurate public framing'}</h2><Card variant="sunken" className="mt-6"><span className="text-micro font-semibold uppercase tracking-[0.06em] text-ink-secondary">{ko?'한 줄':'One line'}</span><p className="mt-2 text-secondary text-ink-primary">{ko?'itriX는 비용이 큰 추론 전에 무엇을 관측해야 하는지, 그리고 실행 전에 적합한 계산을 어떻게 표현해야 하는지를 검토하는 컴퓨팅 AI 인프라를 개발합니다. 모든 적용 가능성과 이점은 사례별 검증 대상입니다.':`${brand.wordmark} builds computational AI infrastructure that examines observation before expensive reasoning and representation before execution; applicability and any advantage are validated case by case.`}</p></Card><Card variant="sunken" className="mt-4"><span className="text-micro font-semibold uppercase tracking-[0.06em] text-ink-secondary">{ko?'문단':'Paragraph'}</span><p className="mt-2 text-secondary text-ink-secondary">{ko?'itriX의 현재 제품은 ASTOP, ALPHA Compute, ALPHA Core입니다. ASTOP은 PRISM 관측 영역을 제품화한 관측 제품이고, ALPHA Compute는 적합한 워크로드를 위한 독립 소프트웨어 계산 인프라 제품이며, ALPHA Core는 근거가 정당화할 때만 고려하는 별도의 선택적 하드웨어 제품입니다. PRISM, AXIOM, AXIOM-TENSOR, CRE, FQNM, QNTA는 기술이며 적용 범위와 근거 경계가 다릅니다.':'itriX currently has three products: ASTOP, ALPHA Compute and ALPHA Core. ASTOP is the observation product operationalizing the PRISM observation domain; ALPHA Compute is the independent software computational infrastructure product for eligible workloads; and ALPHA Core is a separate optional hardware product considered only when evidence justifies it. PRISM, AXIOM, AXIOM-TENSOR, CRE, FQNM and QNTA are technologies with distinct applicability and evidence boundaries.'}</p></Card><p className="mt-6 text-caption text-ink-secondary">{w.mechanism} {w.results}</p></div></section>}
