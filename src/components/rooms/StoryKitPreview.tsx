'use client';
import { Card } from '@/components/ui/Card';import { SectionLabel } from '@/components/ui/SectionLabel';import { brand } from '@/constants/brand';import { useLocaleStore } from '@/store/localeStore';
export function StoryKitPreview(){const ko=useLocaleStore(s=>s.locale)==='ko';return <div className="mx-auto flex max-w-reading flex-col gap-4"><Card variant="default" className="flex flex-col gap-2"><SectionLabel>{ko?'한 줄 프레이밍':'One-line framing'}</SectionLabel><p className="text-web-lead text-structure-900">{ko?'지속 가능한 AI를 위한 컴퓨팅 AI 인프라.':`${brand.positioning}.`}</p></Card><Card variant="default" className="flex flex-col gap-2"><SectionLabel>{ko?'문단 프레이밍':'Paragraph framing'}</SectionLabel><p className="reading text-ink-primary">{ko?'itriX는 지속 가능한 AI를 위한 컴퓨팅 AI 인프라를 개발합니다. 비효율적인 계산을 그대로 확장하기보다, 실행 전에 워크로드를 더 적합한 형태로 재구성할 수 있는지 검토합니다. ALPHA Compute는 표현 가설을 정의하고, ALPHA Core는 그 가설이 목표 실행 환경에서 유용하게 실행될 수 있는지 검증합니다. 모든 이점은 약속이 아니라 평가 대상입니다.':'itriX builds computational AI infrastructure for sustainable AI. Instead of scaling inefficient computation as-is, it asks whether an eligible workload can be reconstructed into a more suitable form before it runs. ALPHA Compute defines the representation hypothesis; ALPHA Core validates whether that hypothesis can run usefully in a target execution environment. Any advantage is evaluated, not promised.'}</p></Card><Card variant="warm" className="flex flex-col gap-2"><SectionLabel tone="gold">{ko?'공개 범위와 제한 범위':'Public and restricted boundaries'}</SectionLabel><p className="reading text-ink-secondary">{ko?'공개: itriX가 무엇을 하고 왜 하는지, 핵심 명제, FQNM 공개 arXiv 프리프린트. 제한: 검증되지 않은 벤치마크 수치, 보호된 메커니즘, 내부 가격 정책, 내부 특허 참조. NDA는 승인된 공개를 보호할 수 있지만 그 자체로 접근 권한을 만들지는 않습니다.':'Public: what itriX does and why, the thesis, and the public FQNM arXiv preprint. Restricted: non-public benchmark detail, protected mechanisms, internal pricing policy and internal patent references. An NDA may protect an authorized disclosure; it does not itself authorize access.'}</p></Card><p className="text-caption italic text-ink-secondary">“{brand.thesis}”</p></div>}
