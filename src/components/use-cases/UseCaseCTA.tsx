'use client';
import Link from 'next/link';import { Card } from '@/components/ui/Card';import { Button } from '@/components/ui/Button';import { SectionLabel } from '@/components/ui/SectionLabel';import { ConfidentialityNote } from '@/components/center/ConfidentialityNote';import { useCaseCopy } from '@/lib/i18n/useCasesLocale';import { useLocaleStore } from '@/store/localeStore';import type { UseCase } from '@/lib/content/useCases';
export function UseCaseCTA({useCase}:{useCase:UseCase}){const locale=useLocaleStore(s=>s.locale);const c=useCaseCopy(locale,useCase);const ko=locale==='ko';return <section className="container-page py-14"><div className="mx-auto flex max-w-reading flex-col gap-6"><div className="flex flex-col gap-3"><SectionLabel>{ko?'itriX가 접근하는 방식':'How itriX would approach it'}</SectionLabel><p className="reading text-ink-primary">{c.approach}</p></div><Card variant="warm" className="flex flex-col gap-2"><SectionLabel withRule={false}>{ko?'어디서 시작할 수 있나':'Where it can start'}</SectionLabel><p className="text-body text-ink-primary">{c.startsWith}</p></Card><div className="flex flex-col gap-3"><Link href={c.ctaHref}><Button variant="primary" size="lg">{c.ctaLabel}</Button></Link><ConfidentialityNote/></div></div></section>}
