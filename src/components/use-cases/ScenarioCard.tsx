'use client';
import Link from 'next/link';import { Card } from '@/components/ui/Card';import { useCaseCopy } from '@/lib/i18n/useCasesLocale';import { useLocaleStore } from '@/store/localeStore';import type { UseCase } from '@/lib/content/useCases';
export function ScenarioCard({useCase}:{useCase:UseCase}){const locale=useLocaleStore(s=>s.locale);const c=useCaseCopy(locale,useCase);return <Link href={`/use-cases/${useCase.slug}`} className="group block"><Card variant="default" interactive className="flex h-full flex-col gap-2"><span className="text-micro font-semibold uppercase tracking-[0.1em] text-ink-primary">{c.audience}</span><h3 className="text-web-h3 text-structure-900 group-hover:text-ink-primary">{c.headline}</h3><p className="text-secondary text-ink-secondary line-clamp-3">{c.situation}</p><span className="mt-auto pt-2 text-secondary font-medium text-ink-primary">{locale==='ko'?'시나리오 보기 →':'Read this scenario →'}</span></Card></Link>}
