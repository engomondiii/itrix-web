import type { ClientPage } from '@/types/client.types';

/** The hidden pitch-room projection is retired from the customer-facing My Review. */
export function PitchSlideDeck(_props: { page: ClientPage }) { return null; }
