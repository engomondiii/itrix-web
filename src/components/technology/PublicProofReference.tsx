'use client';
import type { ReactNode } from 'react'; import { Card } from '@/components/ui/Card'; import { Badge } from '@/components/ui/Badge'; import { useLocaleStore } from '@/store/localeStore';
export interface PublicProofReferenceProps { title: ReactNode; reference: string; href?: string; note?: ReactNode; }
export function PublicProofReference({title,reference,href,note}:PublicProofReferenceProps){const ko=useLocaleStore(s=>s.locale)==='ko';return <Card variant="default" className="flex flex-col gap-2"><div className="flex items-center justify-between gap-3"><span className="text-card-title text-ink-primary">{title}</span><Badge tone="success">{ko?'공개':'Public'}</Badge></div><span className="font-mono text-secondary text-ink-primary">{reference}</span>{note?<span className="text-caption text-ink-secondary">{note}</span>:null}{href?<a href={href} target="_blank" rel="noopener noreferrer" className="mt-1 text-secondary text-ink-primary underline-offset-2 hover:underline">{ko?'출처 보기 ↗':'View source ↗'}</a>:null}</Card>}
